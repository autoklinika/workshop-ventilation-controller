# WVC eMMC wear protection.
# Domain history is persisted on /srv/wvc-data (NVMe). System diagnostics are
# deliberately runtime-only so a missing NVMe can never make journald fall back
# to persistent writes on eMMC.
[Journal]
Storage=volatile
RuntimeMaxUse=64M
RuntimeMaxFileSize=8M
