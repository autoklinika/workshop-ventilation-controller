# Workshop Ventilation Controller — prywatny DHCP dla węzłów KAmod/SEN55.
# DNS jest wyłączony. Klienci nie otrzymują bramy domyślnej.

port=0
interface=wlan0
bind-dynamic
dhcp-authoritative

dhcp-range=10.55.0.100,10.55.0.119,255.255.255.0,12h

# Puste opcje zapobiegają wysyłaniu default gateway i DNS.
dhcp-option=option:router
dhcp-option=option:dns-server

# Lease'y są stanem ulotnym. Trzymamy je w /run (tmpfs), żeby okresowe
# odnowienia DHCP nie generowały zapisów na eMMC. Po restarcie węzły
# automatycznie odtworzą lease'y.
dhcp-leasefile=/run/wvc-sensor-service/dnsmasq-wvc.leases
log-dhcp
