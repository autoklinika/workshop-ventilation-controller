# Workshop Ventilation Controller
# Short POWER button presses are intentionally ignored while Linux is running.
# Normal shutdown/restart must go through wvc-host-power and the SAFE sequence.
# The CM5 hardware >5 s emergency hard-off remains outside systemd-logind.

[Login]
HandlePowerKey=ignore
HandlePowerKeyLongPress=ignore
